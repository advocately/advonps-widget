<div class='advonps-bar'>
  <div class="advonps-bar--close" onclick={{action @close}}>
    ×
  </div>

  <div class="advonps-survey--wrapper">
    {{#if initialRating}}
      <rating-form @submit={{action submitRating}} />
    {{else if leavingFeedback}}
      <feedback-form @rating={{rating}} @submit={{action submitFeedback}} />
    {{else if completedForm}}
      <thank-you />
    {{/if}}
  </div>

  <powered-by />
</div>
